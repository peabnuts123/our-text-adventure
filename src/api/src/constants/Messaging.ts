const Messaging = {
  /** When attempting to look up a command (by string) and none is found */
  NoCommandFound: 'Invalid or unknown command.',

  /** When an action is attempted but a required item is missing */
  RequiredItemNotPresent: 'You lack a required item.',
};

export default Messaging;
