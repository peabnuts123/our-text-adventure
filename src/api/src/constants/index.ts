/**
 * Max number of characters allowed per line for game screens.
 * @NOTE: Must match `TERMINAL_CHARACTER_WIDTH` in `src/www/src/constants/index.ts`
 */
export const TERMINAL_MAX_LINE_LENGTH = 30;

/**
 * Max number of a characters allowed for an item name
 * @NOTE: Must match `ITEM_NAME_MAX_LENGTH` in `src/www/src/constants/index.ts`
 */
export const ITEM_NAME_MAX_LENGTH = 25;
